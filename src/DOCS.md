# Grafana docs RAG pipeline

## Prerequisites

- Python 3.11 or higher
- pip
- git
- ollama

## Building the RAG dataset

1. Clone the Grafana documentation:  

    ```bash
    ./clone-docs.sh
    ```

2. Install required Python packages (preferably in a virtual environment):

    ```bash
    pip install -r requirements.txt
    ```
